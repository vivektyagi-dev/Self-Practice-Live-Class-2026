# OS / editor noise
.DS_Store
Thumbs.db
.vscode/
.idea/

# Claude Code local session logs (not class content)
.claude/

# Python
__pycache__/
*.pyc
.venv/
.venv*/

# Secrets — every class folder ships a `.env.example` instead
.env
.env.*
!.env.example
!*.example.env

# Notebook checkpoints
.ipynb_checkpoints/

# Pre-restructure local scaffolding — superseded by the dated Weekend NN folders,
# kept on disk locally but never tracked (only stray .venv/.env content lives in these)
/Class 1 Python AI/
/Class 4 & 5 and 6.5  - Agents in Python/
/Class 6  - Langchain Starts/
/Class 7 - Langchain/
/class 9 - Langchain Agents + Project/
/class2v2/
